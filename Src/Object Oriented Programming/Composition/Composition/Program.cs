using System;

namespace Composition
{
    class Program
    {
        static void Main(string[] args)
        {
            //Composition Like Dependency-Injection :)  -Code reuse, Flexibility, Loose-coupling -- Composition like Has Relationship
            // In mY opinion Composition > Inheritance
            // Inheritance (IS-A)
            // Composition (Has-A)
        }
    }
}